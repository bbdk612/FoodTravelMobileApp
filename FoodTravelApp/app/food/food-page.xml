<Page xmlns="http://schemas.nativescript.org/tns.xsd"
  navigatingTo="navigatingTo">
  <ActionBar title="{{ food.title }}" />

  <ScrollView>
    <StackLayout>
      <Image margin="10" stretch="aspectFill" src="{{ food.photo }}" />
      <Label text="{{ food.price }}" />
    </StackLayout>
  </ScrollView>
</Page>
